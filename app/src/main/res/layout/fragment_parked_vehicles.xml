<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <TableRow
        android:id="@+id/tableRowTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">
        <TextView
            android:id="@+id/textNumberTitle"
            android:layout_width="20dp"
            android:layout_height="wrap_content"
            android:text="No"
            android:gravity="center" />

        <!-- License Plate -->

        <!-- Entry Time -->
        <TextView
            android:id="@+id/textLicensePlateTitle"
            android:layout_width="60dp"
            android:layout_height="wrap_content"
            android:text="License Plate"
            android:layout_marginRight="10dp"
            android:gravity="center" />

        <TextView
            android:id="@+id/textEntryTimeTitle"
            android:layout_width="70dp"
            android:layout_height="wrap_content"
            android:text="Entry Time"
            android:layout_marginRight="10dp"
            android:gravity="center" />

        <!-- Exit Time -->
        <TextView
            android:id="@+id/textExitTimeTitle"
            android:layout_width="70dp"
            android:layout_height="wrap_content"
            android:text="Exit Time"
            android:layout_marginRight="10dp"
            android:gravity="center" />

        <!-- Actions -->
        <TextView
            android:id="@+id/actionTitle"
            android:layout_width="145dp"
            android:layout_height="wrap_content"
            android:text="Actions"
            android:gravity="center" />
    </TableRow>
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewParkedVehicles"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@+id/tableRowTitle"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>
